functions:
  remotionMcp:
    handler:
      code: src/mcpWebhook.js
    type: webhook
    path: mcp
    method: POST
    description: Remotion MCP Streamable HTTP compatibility endpoint

settings:
  nodeVersion: 20
  timeout: 120
